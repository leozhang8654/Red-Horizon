# 母舰关卡 开发计划

> 进度标记：✅ 已完成 / ⬜ 待办 / 🔎 验证点 / ❌ 已撤销。本文件随开发进度更新。

---

## ⚠️ 2026-06-17 方案变更：撤销"登陆母舰"，改回正常 Boss 结束
原计划是"打到半残→撞进母舰→切平台跳跃关"。**现已全部撤销**，改成**正常打 Boss 结束**：
- 一阶段打炮台 → 二阶段打核心 → 三阶段 Boss 飞到屏幕中央**不攻击**、打本体血量 → **血归零放爆炸解体动画 + 飞机冲出屏幕 + "任务完成"结算页（空格再来一局）**。
- 已删除：登陆过场/残骸动画/玩家冲撞嵌入/"进入母舰"按钮与切场景逻辑。
- **保留**：隐形护盾(shader 局部点亮)、中央核心阶段(含核心爆炸序列)、三阶段 Boss 回中不攻击。
- 残留未用文件（留着没删，需要再清理）：`mothership/`(空壳平台关)、`boss/art/boss_wreck_frames/`(残骸帧)、`boss/shield_impact.*` 与 `boss/art/shield_hit_frames/`(旧球形命中特效)、根目录 `boss残破序列图.png`/`boss/护盾序列图.png` 等源图。

---

## 🎯 总目标（现行）
飞机关(Red Horizon)做一场完整的三阶段 Boss 战，击毁后正常通关结算。下方"撞进母舰/平台关"内容为**已撤销的旧方案**，仅作存档。

---

## 🗺️ 完整流程

```
飞机关 main.tscn
 ├ 第1波 杂兵 → 第2波 Reaper+机械臂 → 第3波 Boss
 └ Boss 战（MANTIS-LUX 母舰）
     ├ 阶段1：打爆 4 个炮台
     │    · 本体罩【不可破隐形盾】：打本体→红色六边形冲击网格→淡出，不掉血
     │    · 炮台不受盾保护，照常能打爆
     │    · Boss 攻击：环形弹 + 炮台机枪
     ├ 阶段2a：打中央核心（发光圆圈）
     │    · 核心的盾解除→核心可掉血、受击开裂闪光
     │    · 本体其余地方仍是隐形盾（继续冒六边形）
     │    · Boss 攻击：环形弹 + 激光
     ├ 阶段2b：打 Boss 本体血量(500)
     │    · 核心爆掉→全盾解除→本体开始掉血
     │    · 环形弹随核心一起停（源头没了），只剩激光
     ├ 过场（撞破登陆）：
     │    · 本体血归零→小规模连环爆炸→Boss 变【半残残骸】(用户提供动画)、停火、留在原地
     │    · 玩家飞机被接管→冲刺撞进 Boss→机头嵌入体内、机尾露在外面
     └ 飞机关结算页：
          · 半透明盖在"飞机嵌住残骸"的画面上
          · 全套演出（得分/用时/评级 S·A·B/盖章/历史最高分）
          · "进入母舰"按钮（按钮 或 空格都行）
               └→ 切场景 → 母舰平台关 mothership.tscn

母舰平台关 mothership.tscn （2D 平台跳跃，从零做）
 └ 通关 → 最终结算（合并飞机关+平台关成绩，避免重复盖章）
```

---

## 📋 分阶段任务清单（含进度）

### ✅ 已完成（地基 + 修正）
- ✅ 空壳母舰关场景 `mothership/mothership.tscn` + `mothership/mothership.gd`（按空格可回飞机关）
- ✅ 把"屏幕适配铁律（绝不加 stretch、用满铺锚点）"写进 `CLAUDE.md`（解决画面错位）

### ✅ M1 — 隐形护盾（已完成，待验证）
- ✅ 护盾=贴合机身的六边形罩 `boss/boss护盾.png`(1448×1086，与 body_full 同尺寸精确重叠)，用 shader `boss/shield.gdshader`：**平时隐身(`base_alpha`=0)，只在被击打处局部显形并提高不透明度(`hit_opacity`)让那块更实更清楚**
- ✅ `boss/boss.gd`：本体开局即侦测子弹；打本体(非炮台/核心弹道)→挡下子弹 + 就近点亮护盾、不掉血；命中点逐渐淡掉
- ✅ 挡弹例外：子弹竖直弹道正对活炮台/暴露核心→放行去打它们
- 🔎 验证点：打机身→只亮命中处的六边形罩并淡出、子弹被挡；打炮台/核心那列→放行打得到；炮台全爆后仍能打通
- 旋钮：`shield.gdshader` 的 `reveal_radius`(点亮范围)/`glow`(亮度)；`boss.gd` 的 `shield_reveal_decay`(淡多快)/`shield_turret_skip`/`core_hit_skip`(放行弹道宽)
- 注：旧的球形/六边形命中特效(`shield_impact.*`、`shield_hit_frames/`)已弃用不再调用

### ✅ M2 — 中央核心阶段（2a）（已完成，待验证）
- ✅ `boss/boss_core.gd` + `boss/boss.tscn`：在 RingCore 处加**可见核心**（程序画占位发光圆）+ 受击判定 + `core_hp`
- ✅ `boss/boss.gd`：阶段1结束→暴露核心可打；核心掉血+闪白+越伤越红越裂；激光在核心阶段也放
- ✅ 核心爆掉→停环形弹、全盾解除（本体可掉血）、进入阶段2b
- 🔎 验证点：炮台全爆后核心现身可打、核心爆了环形弹停+本体开始能打、最终仍能打通
- ✅ 核心改用美术图 `boss/art/boss_cut/boss核心.png`(已抠透明)；核心打爆播 8 帧爆炸序列 `boss/art/core_boom_frames/`，停在末帧(废墟)永久留场；核心血量调高到 200
- 旋钮：`boss.gd` 的 `core_hp`(现200)/`core_score`/`core_hit_skip`(现150)；`boss.tscn` 的 `Core/Sprite` scale(现0.3)、`Core/Boom` scale(现0.73)、受击圈半径(现95)；爆炸快慢=SpriteFrames 的 speed(现14)

### ✅ M3 — 本体血量阶段（2b）（已完成，待验证）
- ✅ `boss/boss.gd`：核心爆后开放本体掉血（`max_hp=200`）；血归零→调 `_start_boarding()` 进过场（不再用原"爆炸消失"动画 `_die`）

### ✅ M4 — 撞破登陆过场（已完成，待验证）
- ✅ `boss/boss.gd` `_start_boarding()`：停火停招、清空全场敌弹、船身回正、计入击毁分+停表；本体换成 10 帧半残动画 `boss/art/boss_wreck_frames/`（停末帧=残骸留场）
- ✅ `player/player.gd` `start_ram()`：交出操控、加速冲进 Boss 中心嵌住（过场全程无敌、不响应输入）
- ✅ 时间线：残骸动画(`boarding_wreck_time`=2s)→玩家冲撞(`ram_time`=1.2s)→通知结算
- 🔎 验证点：血归零→清屏→残骸动画→飞机冲进 Boss 嵌住→画面静下来
- 待调：`Wreck` 节点 scale(现4.09)/位置 对齐机身；`ram_accel`/`ram_time` 让飞机正好冲到位

### ✅ M5 — 结算页改造 + 进入按钮（已完成，待验证）
- ✅ `main.tscn`：标题改"突入母舰"；Bg 透明度降到 0.6（透出嵌住画面）；提示"按 空格 进入母舰"
- ✅ `ui/victory.gd`：`on_boarding_done()` 直接亮结算（保留全套演出，不再谢幕冲刺）；**空格**→`_go_mothership()`→切到 `mothership.tscn`（按钮已按要求去掉）
- 🔎 验证点：完整跑通 打Boss→过场→结算（全套）→按空格→进母舰空壳
- 待调：Bg 透明度 0.6

### ⬜ M6 — 母舰平台关玩法（最大阶段，参考官方控制器量身写）
- ⬜ 全局单例 `GameState`（`project.godot` 注册 autoload + 新建 `systems/game_state.gd`）存飞机关成绩
- ⬜ 玩家角色 `CharacterBody2D`（重力 / 左右走 / 跳）
- ⬜ 平台地块 + 碰撞、相机、关卡出口
- ⬜ 全部通关 → 最终结算（合并两关成绩，避免重复盖章）
- 🔎 全程遵守 no-stretch 自适应铁律

---

## 🔒 已锁定的设计决定
- 护盾 = **命中才显形的隐形盾**（红色六边形网格），阶段1也有、不可破、不保护炮台
- 击破顺序：**炮台 → 核心 → 本体血量**；Boss 本体**不被完全炸毁**，变半残残骸
- 护盾期间 **Boss 继续放招**
- 飞机关结算页**全套演出全亮**（评级/盖章/最高分），结尾换成"**进入母舰**"按钮（+空格）
- 核心美术 = **程序画占位发光圆**；Boss 半残动画 = **用户提供**
- **不显示**血条/进度条；结算页**半透明盖在嵌住画面上**
- 全项目 **no-stretch 自适应**，新 UI 用满铺锚点（见 CLAUDE.md「屏幕适配铁律」）

## ❓ 留到后面再定的细节
- 母舰平台关的**关卡设计/美术/玩法目标**（M6 时细聊）
- 最终结算的**两关合并计分公式**
- 母舰关是否**换 BGM**（注意版权遗留）
- 平台关里玩家**死亡/失败**怎么处理

---

## 参考资料（M6 平台关用）
- 官方《2D Platformer Demo》：https://godotengine.org/asset-library/asset/2727
- KidsCanCode 平台角色教程：https://kidscancode.org/godot_recipes/4.x/2d/platform_character/
- 决定：不整包套用模板（避免带进 stretch / 输入冲突），参考官方控制器在本项目里量身写。
